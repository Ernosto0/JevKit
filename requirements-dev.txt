# Development and CI dependencies. Installs the runtime set first.
-r requirements.txt

pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
respx>=0.21          # mock httpx transports without touching a real provider
aiosqlite>=0.20      # exercises PostgresStore against ephemeral SQLite, no live Postgres needed
ruff>=0.5
mypy>=1.10
types-PyYAML>=6.0
