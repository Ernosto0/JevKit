# Base URL of the JevKit API. Leave empty in development to use the Vite proxy.
VITE_API_BASE_URL=

# Client API key sent as X-API-Key. Development convenience only: anything in a
# VITE_ variable is bundled into the browser build and is not a secret.
VITE_API_KEY=
